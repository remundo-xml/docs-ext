{
  "name": "remundo-docs-ext",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs && rsync -a --exclude='.vitepress' docs/ docs/.vitepress/dist/md/ && bash scripts/generate-llms-txt.sh",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
